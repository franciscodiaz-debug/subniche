# Wiring Run — 2026-04-29

## Pull summary
Pulled from origin/develop. Merged admin CRUD system + specifications system.

## Wired
Nothing wired — no frontend consumers existed for any new backend route.

## Still mocked (unchanged from before)
- `MOCK_ACTIVITY` in `components/profile/profile-activity-content.tsx`
- `MOCK_COLLECTIONS` in `components/profile/profile-collections-content.tsx`
- `MOCK_FOR_SALE_ITEMS` in `components/profile/profile-for-sale-content.tsx`
- `MOCK_TRADE_INTERESTS` + `MOCK_WISHLIST_ITEMS` in `components/profile/profile-looking-for-content.tsx`
- `MOCK_PROFILE` in `app/(app)/profile/[username]/layout.tsx`
- `MOCK_LISTING` in `app/(app)/niche/[slug]/listing/[id]/page.tsx`

## Decisions made
- Admin API helper stubs (`lib/api/admin/`) deferred — will be created when the admin page is built.

## Pending (no backend yet)
- All 10 new admin endpoints unwired — by design, admin page doesn't exist yet.

## Action required
- Run `npx prisma generate` to pick up new Prisma types (Specification, SpecificationValue, SpecificationCategoryValue, ListingSpecificationValue, ListingSpecBitmask, ListingCategoryBitmask).
