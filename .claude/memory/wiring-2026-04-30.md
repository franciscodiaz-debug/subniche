# Wiring Run — 2026-04-30

## Pull summary
Pulled from origin/develop. Merged NicheProfile system (new per-niche user identity) and listing category bitmask auto-recompute.

## Wired
- `lib/api/niche-profile.ts` (NEW) — stubs for `getNicheProfile` and `updateNicheProfile`

## Still mocked (unchanged from before)
- `MOCK_ACTIVITY` in `components/profile/profile-activity-content.tsx`
- `MOCK_COLLECTIONS` in `components/profile/profile-collections-content.tsx`
- `MOCK_FOR_SALE_ITEMS` in `components/profile/profile-for-sale-content.tsx`
- `MOCK_TRADE_INTERESTS` + `MOCK_WISHLIST_ITEMS` in `components/profile/profile-looking-for-content.tsx`
- `MOCK_PROFILE` in `app/(app)/profile/[username]/layout.tsx`
- `MOCK_LISTING` in `app/(app)/niche/[slug]/listing/[id]/page.tsx`

## What was built
- `GET /api/niche/{nicheSlug}/user/{displayName}` — public niche profile with paginated listings
- `PATCH /api/niche/{nicheSlug}/user` — authenticated upsert (creates on first call)
- `NicheProfilePublic` shape: { display_name, bio, user: UserPublic, niche: NichePublic, listings: PaginatedResult<ListingSummary> }
- Internal: listing.service.ts now auto-recomputes category bitmask on create/update (no API change)

## Decisions made
- Created API stubs in `lib/api/niche-profile.ts` (user confirmed)

## Pending (no frontend page yet)
- `app/(app)/niche/[slug]/user/[displayName]/page.tsx` — profile view page not built
- Niche profile edit UI not built
- `see-listing-screen.tsx` seller link still points to global profile — no breaking change, opportunity to upgrade

## Action required
- Run `npx prisma generate` — NicheProfile model added to schema, Prisma client out of date
  (16 TS errors in server/ files until this runs — NONE in frontend files)