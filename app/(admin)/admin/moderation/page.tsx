import { ModerationPage } from "@/components/admin/moderation/moderation-page"

export const metadata = {
  title: "Moderation — SubNiche Admin",
}

export default function AdminModerationPage() {
  return <ModerationPage />
}
