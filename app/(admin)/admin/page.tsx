import { AdminDashboard } from "@/components/admin/dashboard/admin-dashboard"

export const metadata = {
  title: "Dashboard — SubNiche Admin",
}

export default function AdminPage() {
  return <AdminDashboard />
}
