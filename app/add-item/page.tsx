import { PageShell } from "@/components/layout/page-shell";
import { SectionHeader } from "@/components/ui/section-header";

export default function AddItemPage() {
  return (
    <PageShell>
      <SectionHeader
        eyebrow="Create Listing"
        title="Add item placeholder"
        description="The future flow will support For Sale, For Trade, In Collection, Wishlist/Wanted, and valid combinations. No real form is built in this branch."
      />
    </PageShell>
  );
}
